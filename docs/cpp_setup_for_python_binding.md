```
namespace visualize {
    class tensor {
    private:
        // internal data

    public:
        // constructors, show(), etc.

    };

    // operations between tensors
}

extern "C" {

    // C functions to call with python

}
```